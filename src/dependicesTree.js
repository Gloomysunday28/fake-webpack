class DependiciesTree {
  constructor() {
    this.baseURL = process.cwd()
    this.dependiciesMap = Object.create(null)
  }
  getDependiciesTree(exports, dirname) {
    for (let key in )
  }
}

module.exports = new DependiciesTree()